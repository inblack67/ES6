let colors = ['red', 'blue', 'green'];

// on time for every element
colors.forEach(x => console.log(x))

console.log(colors);

// sum of elemtents in the array
let numbers = [1,2,3,4,5];
let sum=0;
numbers.forEach(x => sum += x)
console.log(sum);